# Frontend Environment Variables (place in frontend/.env)
VITE_BACKEND_URL=http://localhost:3001

# Backend Environment Variables (place in backend/.env)
PORT=3001
NODE_ENV=production
CORS_ORIGIN=https://your-frontend-domain.vercel.app